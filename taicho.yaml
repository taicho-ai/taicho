# taicho workspace config. Servers under `mcp.servers` connect at every boot ("always running").
# Firecrawl is added automatically when FIRECRAWL_API_KEY is set; /mcp-added servers persist too.
mcp:
  enabled: true
