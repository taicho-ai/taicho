# @taicho-ai/agent

The reusable single-agent execution kernel: model loop, prompt assembly, context compaction,
live-plan injection, request timeouts, step events, spend ports, and OpenTelemetry instrumentation.
All tools and persistence are supplied by the host.
