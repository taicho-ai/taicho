# @taicho/telemetry

Standard OpenTelemetry tracing and metrics for agent systems, exported over OTLP. It preserves
active context across per-agent tracer providers and has no dependency on the Taicho framework.
