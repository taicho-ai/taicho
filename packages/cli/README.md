# @taicho/cli

The Taicho executable and Ink terminal interface. This package composes `@taicho/framework` with
the standard telemetry package and owns process startup, user interaction, and graceful shutdown.
